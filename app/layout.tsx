import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Data Science & ML Consulting | Advanced Analytics Solutions",
  description:
    "We help businesses unlock the full potential of their data through advanced analytics and machine learning solutions. 15+ years of experience across fintech, telecom, automotive, and e-commerce.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

